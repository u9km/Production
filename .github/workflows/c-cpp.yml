name: AmarShield Zeus - Production Build

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Zero-Trace Dylib
    runs-on: macos-latest

    env:
      THEOS: ${{ github.workspace }}/theos
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      HOMEBREW_NO_AUTO_UPDATE: 1

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: 📦 Cache Theos
        id: cache-theos
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/theos
          key: ${{ runner.os }}-theos-master-v2

      - name: ⚙️ Setup Theos Environment
        if: steps.cache-theos.outputs.cache-hit != 'true'
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"

      - name: 🛠️ Install Dependencies (ldid)
        run: brew install ldid

      - name: 🔍 Verify Source Code (Anti-Cache Check)
        run: |
          echo "Checking if the code is the NEW Camouflaged version..."
          cat AmarShield_Zeus.mm | grep "CoreMemUtils" || (echo "❌ OLD CODE DETECTED! GitHub didn't sync!" && exit 1)
          echo "✅ New Code Confirmed."

      - name: 🚀 Compile AmarShield Zeus (Release Mode)
        run: |
          make clean
          make package FINALPACKAGE=1
        
      - name: 🕵️‍♂️ Extract Dylib
        run: |
          mkdir -p Final_Dylib
          find .theos/obj -name "*.dylib" -type f -exec cp {} Final_Dylib/ \;
          
          echo "====================================="
          echo "🛡️ Dylib Compilation Status:"
          echo "====================================="
          ls -lh Final_Dylib/*.dylib
          file Final_Dylib/*.dylib

      - name: 📤 Upload Artifact (Dylib Only)
        uses: actions/upload-artifact@v4
        with:
          name: AmarShield-Zeus-2026-Dylib
          path: Final_Dylib/*.dylib
          if-no-files-found: error
